SET( DEFINE
)
SET( INCLUDE
)
SET( LINK
)

#[[
if (MSVC)
	SET( WINDOWS_DEFINE
		_CRT_SECURE_NO_WARNINGS
		WIN32_LEAN_AND_MEAN
	)
	LIST(APPEND DEFINE "${WINDOWS_DEFINE}")
endif()
#]]

create_project(STATIC DEFINE INCLUDE LINK)
